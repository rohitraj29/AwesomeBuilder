spring:
  jpa:
    database: MYSQL
    show-sql: true
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/ab3
    username: root
    password: mypass
    driverClassName: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always